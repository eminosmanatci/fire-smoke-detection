# Fire and Smoke Detection using YOLO

This project implements a computer vision model to detect fire and smoke using the YOLO (You Only Look Once) architecture.

## Features

- Fire and smoke detection
- Integration with Firebase for storing detection logs/alerts
- Supports both image and video processing
- Built using Python, PyTorch, and Ultralytics
